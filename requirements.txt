# Core dependencies for enhanced LLM chatbot
gradio==5.32.0
llama-cpp-python
huggingface-hub>=0.20.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0
numpy>=1.24.3,<2.0.0
requests>=2.31.0,<3.0.0

# Additional dependencies for enhanced functionality
# webbrowser, threading, datetime, typing, json, os, logging, random are built-in modules

# Note: Removed all image analysis dependencies:
# - PIL/Pillow (image processing)
# - transformers vision components
# - torch/torchvision (deep learning)
# - opencv-python (computer vision)
# - ultralytics (YOLO)
# - segment-anything (image segmentation)
# - matplotlib (plotting)
# - diffusers (image generation)
# - accelerate (model acceleration)
# - flask (web framework)
# 
# This streamlined version focuses purely on text-based LLM functionality
# with expanded model compatibility and enhanced user experience.
